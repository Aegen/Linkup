<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aegis.linkup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewUserActivity" />
        <activity android:name=".NewUserActivityTwo" />
        <activity android:name=".AddGamesActivity" />
        <activity android:name=".PreferencesActivity" />
        <activity
            android:name=".MainProfileActivity"
            android:label="@string/title_activity_main_profile" />
        <activity android:name=".ChooseGameActivity" />
        <activity android:name=".PresentMatchActivity"></activity>
    </application>

</manifest>